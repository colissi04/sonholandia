import { AuthHeaderView, PrimaryText, SecondaryText} from './styles';

export function AuthHeader(){
  return(
    <AuthHeaderView>
      <PrimaryText />
      <SecondaryText />
    </AuthHeaderView>
  )
}
